# MongoDB URI (you'll need to replace this with your production MongoDB URI)
MONGODB_URI=mongodb+srv://week4_assignment:week4_assignment@cluster0.zercwoq.mongodb.net/

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Configuration (use a strong secret in production)
JWT_SECRET=your_production_secret_here

# Client URL (update this after deploying frontend)
CLIENT_URL=https://your-app.netlify.app